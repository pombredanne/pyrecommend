- Update recommendations on quote view/favorite
  Relevant code:

      rec.similarity_data(get_ratings.QuoteData(Quote.objects.get(pk=1)),
                          get_ratings.ResultStorage())

- Probably need a bulk_store() method on QuoteSimilarity.
